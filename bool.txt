
# need to rename for $concat3, and make $concat3?

Event = { id = TRUE  $parameter = { x } $return = { $concat3 = { "Event = { id = -1 $parameter = { y } $return = { " $parameter.x " } } " } } }

Event = { id = FALSE $parameter = { x } $return = { "Event = { id = -1 $parameter = { y } $return = { $parameter.y } } " } }

